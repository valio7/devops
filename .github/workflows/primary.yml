name: Primary workflow

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  quality_assurance:
    uses: ./.github/workflows/quality_assurance.yml
    with:
      SONAR_TOKEN: ${{ secrets.SECRET_TOKEN }}
